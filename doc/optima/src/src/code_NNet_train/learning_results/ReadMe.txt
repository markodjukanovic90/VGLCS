Results were obtained by training with the training_parameters.txt and using the instances in training.slurm
Instances .0 were used for getting the training values and the .1 for the validation values. 
The results were obtained by executing with the parameters in parameters.txt, every row is an average for the instances 2, 3 and 4
